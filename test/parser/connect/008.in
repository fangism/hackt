// should pass: types are equivalent
// actually type-checker is lazy and won't check param actuals
// of typedef'd types until unroll time.

int fe;
int<> fi;
int<16> fo;		// different from the rest

typedef	int<32>		int32;

int32 fum = fe = fi = fo;
	// type-checker won't catch type mismatch through 
	// static constant parameter differences... yet


