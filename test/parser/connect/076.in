// "076.in" fail parser: can't instantiate array and connect ports at same time

defproc portaproc(bool b, c[4]; int i, j[2]);

bool a;
bool x[4];
int q;
int r[2];

portaproc p[0..0](a, x, q, r);		// bad declaration

