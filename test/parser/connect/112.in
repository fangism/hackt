// "112.in" elaborate test of connectivity
// copy-extended from "111.in"

defproc wrap(bool a, b) {
}

bool x, y, z, w;

wrap j, k, l, m;

j = k;

l(x, y);
m(x, y);

j.a = k.b;
k.a = z;

m(w, );
l( , w);

z = w;

// should reduce to one unique bool

