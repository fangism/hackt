// "108.in" should fail, types mismatch
// copy-modified from "107.in"

template <pint A>
defproc foo() { }

template <pint X,Y>
defproc bar(foo<X> a; foo<Y> b) { }

bar<2,3> w;
bar<3,4> z;

w.a = z.b;	// should fail, 2 != 4

