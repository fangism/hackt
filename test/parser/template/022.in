// "022.in" should pass: all forward declarations are equivalent

template <pint N; pint A[N]>
defproc foo();

template <pint N; pint B[N]>
defproc foo();

template <pint M; pint A[M]>
defproc foo();

template <pint M; pint B[M]>
defproc foo();

// update grammar to allow anonymous parameters:
// template <pint N; pint [N]> defproc foo;


