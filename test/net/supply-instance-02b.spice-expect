* subcircuit globals<> is empty.

.subckt inv<> !GND !Vdd x y
My:dn:0 !GND x y !GND nch W=5u L=2u
My:up:0 !Vdd x y !Vdd pch W=5u L=2u
.ends

xa !GND !Vdd a.x a.y inv<>
xb !GND !Vdd b.x b.y inv<>
xc !GND !Vdd c.x c.y inv<>
xd d.!GND !Vdd d.x d.y inv<>
xd2 h.!GND !Vdd d2.x d2.y inv<>
xe d.!GND !Vdd e.x e.y inv<>
xf !GND f.!Vdd f.x f.y inv<>
xh h.!GND h.!Vdd h.x h.y inv<>

