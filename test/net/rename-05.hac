// "rename-05.hac"
// copy-modified from "rename-05.hac"
// TROUBLE: haven't handled labels inside complement expansions yet
// can't see a way to cleanly implement, just have to expand manually
// this is now caught by the collision checker

bool x, y, _z, _w;
prs {
	x & y<;"OHAI">		#> _z-
	x<;"drat"> & y		=> _w-
}
