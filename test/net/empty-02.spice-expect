* channel or struct e1of2<> is omitted.

* subcircuit empty_1<> is empty.

* subcircuit empty_2<> is empty.

* subcircuit empty_3<> is empty.

.subckt inv<> !GND !Vdd a b
Mb:dn:0 !GND a b !GND nch W=5u L=2u
Mb:up:0 !Vdd a b !Vdd pch W=5u L=2u
.ends

xbar !GND !Vdd bar.a bar.b inv<>

